apiVersion: v1
kind: Service
metadata:
  name: {{ template "webservice.fullname" . }}
  labels:
    app: {{ template "webservice.name" . }}
    chart: {{ template "webservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.env.WEBSERVICE_PORT }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "webservice.name" . }}
